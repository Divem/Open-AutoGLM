<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal Agent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet">
</head>
<body>
    <div class="main-container">
        <!-- 聊天区域 -->
        <div class="chat-area">
            <div class="chat-header">
                <div class="chat-title">
                    <i class="fas fa-comments me-2"></i>
                    Terminal Assistant
                </div>
                <div class="chat-controls">
                    <span id="connection-status" class="connection-status connected">
                        <i class="fas fa-circle me-1"></i>
                        已连接
                    </span>
                    <span id="session-id" class="session-id">
                        会话ID: 未初始化
                    </span>
                    <button id="screenshot-toggle-btn" class="screenshot-toggle-btn" title="切换截图显示" aria-label="切换实时截图显示状态">
                        <i class="fas fa-eye"></i>
                    </button>
                    <a href="{{ url_for('config') }}" class="config-btn" title="配置">
                        <i class="fas fa-cog"></i>
                    </a>
                </div>
            </div>

            <div class="chat-container" id="chat-messages">
                <div class="welcome-message">
                    <i class="fas fa-robot fa-4x mb-4"></i>
                    <h3>欢迎使用 Terminal Agent</h3>
                    <p>通过自然语言控制您的手机</p>
                    <p class="small">例如："打开微信查看未读消息"</p>
                    <div class="mt-3">
                        <small class="text-muted">
                            <i class="fas fa-mobile-alt me-1"></i>
                            支持 70+ 主流应用 • 实时截图 • 多轮对话
                        </small>
                    </div>
                </div>
            </div>

            <div class="input-area">
                <div class="input-group">
                    <input type="text"
                           class="form-control"
                           id="task-input"
                           placeholder="输入任务描述..."
                           disabled>
                    <button class="btn btn-primary" id="send-btn" disabled>
                        <i class="fas fa-paper-plane me-1"></i>
                        发送
                    </button>
                    <button class="btn btn-warning" id="stop-btn" disabled>
                        <i class="fas fa-stop me-1"></i>
                        停止
                    </button>
                </div>
            </div>
        </div>

        <!-- 右侧：状态和截图区域 -->
        <div class="side-panel">
            <!-- 状态面板 -->
            <div class="status-panel">
                <h6 class="text-muted mb-3">
                    <i class="fas fa-chart-line me-2"></i>
                    执行状态
                </h6>

                <div class="mb-3">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <span class="text-muted">当前任务:</span>
                        <span id="current-task" class="badge bg-secondary">无</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <span class="text-muted">任务状态:</span>
                        <span id="task-status" class="badge bg-secondary">空闲</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <span class="text-muted">执行步数:</span>
                        <span id="step-count">0</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="text-muted">执行时间:</span>
                        <span id="execution-time">0s</span>
                    </div>
                </div>

                <!-- 进度条 -->
                <div class="progress mb-3" style="height: 6px;">
                    <div id="task-progress" class="progress-bar" role="progressbar" style="width: 0%"></div>
                </div>
            </div>

            <div class="side-panel-content">
                <!-- 设备信息 -->
                <div class="device-info">
                    <h6 class="text-muted mb-3">
                        <i class="fas fa-mobile-alt me-2"></i>
                        设备信息
                    </h6>
                    <div id="device-list" class="small">
                        <div class="text-muted">正在加载设备信息...</div>
                    </div>
                </div>

                <!-- 截图区域 -->
                <div class="screenshot-panel">
                    <h6 class="text-muted mb-3">
                        <i class="fas fa-image me-2"></i>
                        实时截图
                    </h6>
                    <div id="screenshot-container" class="screenshot-container">
                        <div class="text-center text-muted py-5">
                            <i class="fas fa-image fa-2x mb-2"></i>
                            <p class="small">执行任务时将显示截图</p>
                        </div>
                    </div>
                </div>

                <!-- 任务历史 -->
                <div class="task-history">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h6 class="text-muted mb-0">
                            <i class="fas fa-history me-2"></i>
                            任务历史
                        </h6>
                        <button class="btn btn-sm btn-outline-secondary" onclick="refreshTaskHistory()">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                    </div>
                    <div id="task-history-list" class="task-history-list">
                        <div class="text-center text-muted">
                            <p class="small">暂无任务历史</p>
                        </div>
                    </div>
                </div>

                <!-- 支持的应用 -->
                <div class="supported-apps">
                    <h6 class="text-muted mb-3">
                        <i class="fas fa-th me-2"></i>
                        支持的应用
                    </h6>
                    <div id="apps-list" class="small">
                        <div class="text-muted">正在加载应用列表...</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 悬浮截图窗口 -->
    <div id="floating-screenshot"
         class="floating-screenshot hidden"
         role="dialog"
         aria-label="实时截图窗口"
         aria-describedby="screenshot-description"
         tabindex="-1">
        <div id="screenshot-description" class="sr-only">
            显示手机操作的实时截图,支持拖拽调整位置
        </div>

        <!-- 拖拽手柄 -->
        <div class="floating-screenshot-header"
             role="button"
             tabindex="0"
             aria-label="拖拽移动截图窗口">
            <div class="screenshot-title">
                <i class="fas fa-mobile-alt me-2"></i>
                实时截图
            </div>
            <div class="screenshot-controls">
                <button class="screenshot-control-btn minimize-btn"
                        title="最小化"
                        aria-label="最小化截图窗口">
                    <i class="fas fa-minus"></i>
                </button>
                <button class="screenshot-control-btn maximize-btn"
                        title="最大化"
                        aria-label="最大化截图窗口">
                    <i class="fas fa-expand"></i>
                </button>
                <button class="screenshot-control-btn close-btn"
                        title="关闭"
                        aria-label="关闭截图窗口">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </div>

        <!-- 截图内容区域 -->
        <div class="floating-screenshot-content">
            <div id="floating-screenshot-container" class="floating-screenshot-container">
                <div class="text-center text-muted py-4">
                    <i class="fas fa-image fa-2x mb-2"></i>
                    <p class="small mb-0">等待截图...</p>
                </div>
            </div>
        </div>

        <!-- 加载状态指示器 -->
        <div class="screenshot-loading-indicator">
            <div class="spinner-border spinner-border-sm" role="status">
                <span class="visually-hidden">加载中...</span>
            </div>
        </div>
    </div>

    <!-- 滚动到底部按钮 -->
    <button id="scroll-to-bottom" class="scroll-btn" title="滚动到底部">
        <i class="fas fa-arrow-down"></i>
        <span class="new-message-indicator">新消息</span>
    </button>

    <!-- Toast 通知 -->
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
        <div id="toast" class="toast" role="alert">
            <div class="toast-header">
                <strong class="me-auto">系统通知</strong>
                <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
            </div>
            <div class="toast-body" id="toast-message">
                通知消息
            </div>
        </div>
    </div>

    <!-- 加载脚本 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.0.1/socket.io.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <script src="{{ url_for('static', filename='js/smart-scroller.js') }}"></script>
    <script src="{{ url_for('static', filename='js/app.js') }}"></script>
</body>
</html>